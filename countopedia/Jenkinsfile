pipeline {
    agent any

    stages {

        stage('Install Dependencies') {
            steps {
                dir('countopedia') {
                    bat 'npm ci'
                }
            }
        }

        stage('Build') {
            steps {
                dir('countopedia') {
                    bat 'npm run build'
                }
            }
        }

        stage('Docker Build') {
            steps {
                dir('countopedia') {
                    bat 'docker build -t countopedia:latest .'
                }
            }
        }

    }
}
